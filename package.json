{
  "name": "stocsy",
  "version": "0.0.1",
  "description": "Implementation of the Statistical Total Correlation Spectroscopy(STOCSY)",
  "main": "./src/index.js",
  "files": [
    "runkit.js",
    "src"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/cheminfo-js/stocsy.git"
  },
  "keywords": [
    "machine",
    "learning",
    "data",
    "mining",
    "datamining"
  ],
  "author": "andcastillo <andcastillo@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/cheminfo-js/stocsy/issues"
  },
  "runkitExampleFilename": "./runkit.js",
  "homepage": "https://github.com/cheminfo-js/stocsy#readme",
  "scripts": {
    "eslint": "eslint src test",
    "eslint-fix": "npm run eslint -- --fix",
    "test": "npm run test-mocha && npm run eslint",
    "test-cov": "istanbul cover node_modules/.bin/_mocha -- --require should --reporter dot --recursive",
    "test-travis": "istanbul cover node_modules/.bin/_mocha --report lcovonly -- --require should --reporter dot --recursive",
    "test-mocha": "mocha --require should --reporter mocha-better-spec-reporter",
    "build": "cheminfo build"
  },
  "devDependencies": {
    "cheminfo-tools": "^1.5.0",
    "eslint": "^3.4.0",
    "eslint-config-cheminfo": "^1.2.0",
    "eslint-plugin-no-only-tests": "^1.1.0",
    "istanbul": "^0.4.4",
    "mocha": "^3.1.2",
    "mocha-better-spec-reporter": "^3.0.2",
    "openchemlib-extended": "1.12.3"
  },
  "dependencies": {
    "ml-pca": "^1.1.2",
    "ml-matrix": "^2.1.0",
    "nmr-predictor": "^0.1.0",
    "nmr-simulation": "^0.1.4",
    "brukerconverter": "^1.0.1"
  }
}
